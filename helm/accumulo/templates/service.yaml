apiVersion: v1
kind: Service
metadata:
  name: {{ include "accumulo.fullname" . }}
  labels:
    {{- include "accumulo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "accumulo.selectorLabels" . | nindent 4 }}
